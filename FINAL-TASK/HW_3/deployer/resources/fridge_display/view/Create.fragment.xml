<core:FragmentDefinition
   xmlns="sap.m"
   xmlns:core="sap.ui.core"
   xmlns:l="sap.ui.layout"
   xmlns:tnt="sap.tnt">
   <Dialog title="{i18n>createDialogTitle}" id="createDialog">
        <l:VerticalLayout>
            <Label text="{i18n>userNameLabelText}" class="sapUiSmallMarginBegin"/>
            <Input value="{fridgeModel>/bname}" required="true" width="300px" class="sapUiSmallMarginBegin sapUiSmallMarginEnd"/>
            <Label text="{i18n>fridgeCapacityText}" class="sapUiSmallMarginBegin"/>
            <Input value="{fridgeModel>/cap}" required="true" width="300px" class="sapUiSmallMarginBegin sapUiSmallMarginEnd"/>
        </l:VerticalLayout>
        <beginButton>
            <Button icon="sap-icon://accept" type="Accept" press=".createButton" class="roundClass"/>
            </beginButton>
            <endButton>
            <Button icon="sap-icon://decline" type="Reject" press=".closeDialog" class="roundClass"/>
            </endButton>
   </Dialog>
</core:FragmentDefinition>